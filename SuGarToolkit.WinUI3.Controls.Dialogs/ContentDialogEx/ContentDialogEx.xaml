<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:layout="using:SuGarToolkit.WinUI3.Layout"
	xmlns:local="using:SuGarToolkit.WinUI3.Controls.Dialogs">

	<Style TargetType="local:ContentDialogEx">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="local:ContentDialogEx">
					<layout:HeaderBodyFooterView
						HorizontalContentAlignment="Stretch"
						VerticalContentAlignment="Stretch"
						Background="{TemplateBinding Background}"
						BorderBrush="{TemplateBinding BorderBrush}"
						BorderThickness="{TemplateBinding BorderThickness}"
						HorizontalFooterAlignment="Right"
						VerticalFooterAlignment="Center">
						<layout:HeaderBodyFooterView.Footer>
							<ScrollViewer
								x:Name="PART_CommandSpaceScrollViewer"
								HorizontalScrollBarVisibility="Auto"
								VerticalScrollBarVisibility="Disabled">
								<ItemsControl
									x:Name="PART_CommandSpaceItemsControl"
									Margin="{ThemeResource ContentDialogPadding}"
									ItemsSource="{TemplateBinding ButtonItemsSource}">
									<ItemsControl.ItemsPanel>
										<ItemsPanelTemplate>
											<StackPanel Orientation="Horizontal" Spacing="8" />
										</ItemsPanelTemplate>
									</ItemsControl.ItemsPanel>
								</ItemsControl>
							</ScrollViewer>
						</layout:HeaderBodyFooterView.Footer>
						<ScrollViewer
							Grid.Row="0"
							Background="{ThemeResource ContentDialogViewTopOverlay}"
							BorderBrush="{ThemeResource ContentDialogViewSeparatorBorderBrush}"
							BorderThickness="{ThemeResource ContentDialogSeparatorThickness}"
							HorizontalScrollBarVisibility="Disabled"
							IsTabStop="False"
							VerticalScrollBarVisibility="Auto"
							ZoomMode="Disabled">
							<Grid>
								<Grid.RowDefinitions>
									<RowDefinition Height="Auto" />
									<RowDefinition Height="*" />
								</Grid.RowDefinitions>
								<Border
									x:Name="TitleArea"
									Grid.Row="0"
									Padding="{ThemeResource ContentDialogViewTitlePadding}"
									HorizontalAlignment="Stretch"
									x:FieldModifier="private">
									<ContentControl
										x:Name="Title"
										Margin="{ThemeResource ContentDialogTitleMargin}"
										HorizontalAlignment="Left"
										VerticalAlignment="Top"
										Content="{TemplateBinding Header}"
										ContentTemplate="{TemplateBinding HeaderTemplate}"
										FontFamily="{ThemeResource ContentControlThemeFontFamily}"
										FontSize="20"
										FontWeight="SemiBold"
										Foreground="{TemplateBinding Foreground}"
										IsTabStop="True">
										<ContentControl.Template>
											<ControlTemplate TargetType="ContentControl">
												<ContentPresenter
													Margin="{TemplateBinding Padding}"
													HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
													VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
													Content="{TemplateBinding Content}"
													ContentTemplate="{TemplateBinding ContentTemplate}"
													ContentTransitions="{TemplateBinding ContentTransitions}"
													MaxLines="2"
													TextWrapping="Wrap" />
											</ControlTemplate>
										</ContentControl.Template>
									</ContentControl>
								</Border>
								<ContentPresenter
									x:Name="ContentArea"
									Grid.Row="1"
									Padding="{ThemeResource ContentDialogViewContentPadding}"
									Content="{TemplateBinding Content}"
									ContentTemplate="{TemplateBinding ContentTemplate}"
									FontFamily="{ThemeResource ContentControlThemeFontFamily}"
									FontSize="{ThemeResource ControlContentThemeFontSize}"
									Foreground="{TemplateBinding Foreground}"
									TextWrapping="Wrap" />
							</Grid>
						</ScrollViewer>
					</layout:HeaderBodyFooterView>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
